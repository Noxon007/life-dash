# Nur für Tests und Prüfläufe — NICHT im Image (das installiert
# requirements.txt, und zwar nur die).
#
# **Anmerkung 223: `pytest` ist hierher gewandert.** Es stand in
# requirements.txt, begründet damit, der Invarianten-Test (P2.6) solle „auch in
# einem Container laufen können". Der Grund trägt nicht: das Image kopiert
# `backend/app`, `backend/modules` und `frontend` — **die Tests sind gar nicht
# darin**. Ausgeliefert wurde also ein Testwerkzeug ohne Tests. Wer sie im
# Container fahren will, braucht ohnehin diese Datei UND das Testverzeichnis.
#
# Der Treiber steht seit derselben Runde in requirements.txt: die Vorgabe der
# Compose-Datei ist PostgreSQL, also gehört er zum Betrieb.
-r requirements.txt

pytest==9.1.1

# Zeigt, wohin die Testdateien NICHT schauen.
pytest-cov==6.0.0

# `fastapi.testclient.TestClient` steht darauf — ohne httpx scheitert schon der
# IMPORT, also die Sammelphase, und pytest bricht mit Code 2 ab, bevor ein
# einziger Test lief. Lokal war es zufällig mitinstalliert und fiel deshalb
# erst in der CI auf (F20/F21 sind die ersten Tests hier, die einen Client
# bauen). Nur hier und nicht in requirements.txt: das Image braucht keinen
# Testclient.
httpx==0.28.1
